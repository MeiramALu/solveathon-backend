# 💧 AI-Sana Shakarim Solveathon: AI & Agriculture-2025

## 🎯 Цель проекта

Разработать AI-систему управления водными ресурсами для хлопковых полей.
Задача — создать алгоритм, который на основе данных IoT-сенсоров и прогноза погоды определяет **оптимальное время и объём полива**, чтобы максимизировать урожайность (`CottonYield`) при минимизации расхода воды.

---

# 📂 Структура датасета

```
root/
│
├── CottonYield.csv             # 🎯 ЦЕЛЕВАЯ МЕТРИКА: Результаты урожайности
├── ManagementInfo.csv          # 🚜 ЖУРНАЛ АГРОНОМА: История поливов и фазы роста
│
├── Weather/
│   └── Weather.csv             # ☁️ МЕТЕОСТАНЦИЯ: Глобальные погодные данные (посуточные)
│
└── CottonSensor/               # 📡 IOT ДАННЫЕ (Временные ряды с датчиков):
    ├── Sensors.csv             # 🗺️ КАРТА СЕНСОРОВ: Метаданные (ID датчика <-> Координаты)
    ├── HumiditySensor.csv      # 💧 Влажность почвы (Главный параметр)
    ├── TemSensor.csv           # 🌡️ Температура почвы
    ├── HeatFluxSensor.csv      # ♨️ Тепловой поток (испарение влаги)
    ├── ConductSensor.csv       # ⚡ Электропроводность (Засоление/Удобрения)
    ├── PHSensor.csv            # 🧪 Кислотность почвы (pH)
    ├── SoilNutSensor.csv       # 💊 NPK: Азот, Фосфор, Калий
    ├── Meterological.csv       # 🌬️ Микроклимат на поле
    └── Overview.csv            # ℹ️ Техническая сводка
```

---

# 🛠 Подробное описание данных

## 1. Корневая папка (Результаты и Управление)

### **CottonYield.csv** (Целевая переменная / Reward)

Содержание: Итоги урожайности, собранные по квадрантам.

* `treatment`: ID экспериментального участка (Z1, T1 и т.д.)
* `lint_cotton_yield(kg/mu)`: Урожайность чистого хлопкового волокна (кг/му)
* `seed_cotton_yield(kg/mu)`: Урожай хлопка-сырца

---

### **ManagementInfo.csv** (Действия / Actions)

Журнал действий агронома, включая полив и внесение удобрений.

* `process_batch`: ID участка (T1)
* `irrigation_time`: Дата действия
* `reproductive_period`: Фаза роста
* `irrigation_amount(m3/mu)`: Объём воды
* `N(kg/hm2)`, `P2O5(kg/hm2)`, `K2O(kg/hm2)`: Удобрения (кг/га)

---

## 2. Папка Weather (Внешняя среда)

### **Weather.csv**

Глобальные метеоданные (шаг — 1 день):

* `date`
* `rain(mm/day)`
* `wind(km/d)`
* `daily_mean_temperature(°C)`
* `tmax(°C)`
* `tmin(°C)`
* `srad(MJ/(m2*day))`

---

## 3. Папка CottonSensor (IoT Мониторинг)

Все файлы — временные ряды.

### **Sensors.csv** — Ключ для объединения

* `sensor_id`
* `location_info_x`
* `location_info_y`

---

### Ключевые данные сенсоров:

* **HumiditySensor.csv**: `soil_humidity(%)` — главный параметр
* **TemSensor.csv**: `soil_temperature(°C)`
* **SoilNutSensor.csv**: `N(mg/kg)`, `P(mg/kg)`, `K(mg/kg)`
* **ConductSensor.csv**: `soil_conductivity(S/m)`
* **PHSensor.csv**: `ph`
* **HeatFluxSensor.csv**: тепловой поток
* **Meterological.csv**: микроклимат
* **Overview.csv**: техническая сводка

---

# 🔗 Логика решения (Pipeline)

## **Шаг 1: Связка данных (Feature Engineering)**

### Сопоставление сенсора ↔ участок

Использовать:

* координаты из `Sensors.csv`
* временные метки
* скачки влажности после `irrigation_time`

---

### Ресемплинг

Привести временные ряды к единому шагу (например, 1 день).

---

## **Шаг 2: Прогнозирование и Оптимизация**

### Прогноз (Forecasting)

Модель предсказывает `soil_humidity` на 24–72 часа вперёд, используя:

* погоду
* историю полива
* текущие датчики

---

### Рекомендация (Optimization)

Система проверяет прогноз.
Если влажность опускается ниже порога (зависит от `reproductive_period`):

→ выдаёт требуемый объём полива (`irrigation_amount`).

---

# 🚀 Задачи

* Прогноз влажности ≥ **85% точности**
* Алгоритм оптимального расписания полива
* Веб-интерфейс для визуализации