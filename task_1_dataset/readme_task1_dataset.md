# How to run it

1. Do pip install requirements.txt (after creating a virtual env)
2. run python app.py (but you need to improve routing to ports etc, maybe change it to javascript.)
3. open the local ip given to you.
